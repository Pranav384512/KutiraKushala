<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <!-- Fix for CALL_PHONE warning -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false"/>

    <application
        android:allowBackup="true"
        android:label="Kutira-Kushala"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KutiraKushala">

        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"/>

        <activity
            android:name=".DashboardActivity"
            android:exported="false"/>

        <activity
            android:name=".AddProductActivity"
            android:exported="false"/>

        <activity
            android:name=".CapacityMeterActivity"
            android:exported="false"/>

        <activity
            android:name=".ViewProductsActivity"
            android:exported="false"/>
        <activity
            android:name=".BusinessProfileActivity"
            android:exported="false"/>

        <activity
            android:name=".BusinessListActivity"
            android:exported="false"/>

        <activity
            android:name=".BusinessDetailActivity"
            android:exported="false"/>

    </application>
</manifest>